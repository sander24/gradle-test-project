apply plugin: 'java'

version = 0.1

repositories {
  mavenCentral()
}

sourceSets {
    main {
      java {
        // filter out some classes
        exclude 'com/zeroturnaround/gradle/unwanted/**'
      }
    }
}


// execute "gradle clean build"